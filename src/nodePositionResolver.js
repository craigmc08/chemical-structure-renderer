export default function nodePositionResolver(nodes, edges, relativePositions) {
  const unreadNodePositions = [...relativePositions];

/*
SOME PSUEDO CODE/IDEA ON HOW TO DO THIS

calculate node position starting at x,y
  for each node connected to this node
    calculate its relative px position from x,y
    calculate its node position starting at dx,dy

start at the first relativePosition
  calculate its position starting at 0,0
*/
}
